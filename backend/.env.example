# DocTranslator 环境配置示例
# 复制此文件为 .env 并根据实际情况修改配置

# ============ 应用配置 ============
APP_NAME=DocTranslator API
APP_VERSION=1.0.0
DEBUG=True
API_PREFIX=/api

# ============ 安全配置 ============
SECRET_KEY=your-secret-key-change-in-production
JWT_SECRET_KEY=your-jwt-secret-key-change-in-production
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=10080

# ============ 数据库配置 ============
# 开发环境 MySQL
DATABASE_URL=mysql+pymysql://root:password@localhost:3306/doc_translator?charset=utf8mb4
# 生产环境请修改为实际数据库连接
# DATABASE_URL=mysql+pymysql://user:password@host:3306/doc_translator?charset=utf8mb4

# ============ CORS 配置 ============
# 开发环境允许所有源
CORS_ORIGINS=["*"]
# 生产环境应该指定具体域名
# CORS_ORIGINS=["https://yourdomain.com","https://www.yourdomain.com"]

# ============ AI 翻译配置 ============
# OpenAI 兼容 API 配置
OPENAI_API_KEY=your-openai-api-key
OPENAI_API_BASE=https://api.openai.com/v1
OPENAI_MODEL=gpt-3.5-turbo
OPENAI_TIMEOUT=60

# 如果使用国内 API 服务（如通义千问、文心一言等），修改以下配置：
# OPENAI_API_BASE=https://dashscope.aliyuncs.com/compatible-mode/v1
# OPENAI_MODEL=qwen-turbo

# ============ Redis 配置（可选）============
REDIS_URL=redis://localhost:6379/0

# ============ 分页配置 ============
DEFAULT_PAGE=1
DEFAULT_PAGE_SIZE=20
MAX_PAGE_SIZE=100

# ============ 日志配置 ============
# 日志级别：DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO
# 单个日志文件最大大小（字节）默认 10MB
LOG_MAX_BYTES=10485760
# 保留的日志备份文件数量
LOG_BACKUP_COUNT=10

# ============ 文件上传配置 ============
MAX_FILE_SIZE=30
MAX_USER_STORAGE=100

# ============ 邮箱配置（可选）============
MAIL_SERVER=smtp.qq.com
MAIL_PORT=587
MAIL_USE_TLS=true
MAIL_USERNAME=your-email@qq.com
MAIL_PASSWORD=your-mail-authorization-code
MAIL_DEFAULT_SENDER=your-email@qq.com
ALLOWED_EMAIL_DOMAINS=qq.com,163.com,126.com
